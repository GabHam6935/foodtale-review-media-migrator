{
  "name": "foodtale-review-media-migrator",
  "version": "1.0.0",
  "description": "Convert old Foodtale review image media to WebP while preserving review attachments.",
  "main": "src/index.js",
  "scripts": {
    "dry-run": "node -r dotenv/config src/index.js --dry-run",
    "migrate": "node -r dotenv/config src/index.js",
    "verify": "node -r dotenv/config src/verify.js",
    "check": "node --check src/index.js && node --check src/verify.js && node --check src/models.js && node --check src/s3Webp.js && node --check src/utils.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.832.0",
    "dotenv": "^16.4.7",
    "mongoose": "^8.10.0",
    "sharp": "^0.34.5"
  }
}
